// VirtIO Console — placeholder for v0.2

#[allow(dead_code)]
pub struct VirtioConsole;

impl Default for VirtioConsole {
    fn default() -> Self {
        Self::new()
    }
}

#[allow(dead_code)]
impl VirtioConsole {
    pub fn new() -> Self {
        Self
    }
}
