// VirtIO Network Device — placeholder for v0.2

#[allow(dead_code)]
pub struct VirtioNet;

impl Default for VirtioNet {
    fn default() -> Self {
        Self::new()
    }
}

#[allow(dead_code)]
impl VirtioNet {
    pub fn new() -> Self {
        Self
    }
}
